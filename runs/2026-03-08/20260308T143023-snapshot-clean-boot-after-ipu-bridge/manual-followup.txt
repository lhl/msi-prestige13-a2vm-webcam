Source: user terminal transcript pasted on 2026-03-08 after a fresh boot into
the combined-patch kernel with:

- MSI `INT3472` / `TPS68470` board-data patch
- `ipu-bridge` `OVTI5675` patch

Purpose:

- verify whether the earlier dummy-regulator warnings were real on a clean boot
- determine the next concrete blocker after the `ipu-bridge` fix

Commands and observed results:

1. Check the most relevant boot log lines:

   `journalctl -b -k --no-pager | rg 'TPS68470 REVID|Found supported sensor|Connected 1 cameras|supply avdd|supply dovdd|supply dvdd|failed to find sensor|probe with driver ov5675 failed'`

   Observed lines:

- `intel-ipu7 0000:00:05.0: Found supported sensor OVTI5675:00`
- `intel-ipu7 0000:00:05.0: Connected 1 cameras`
- `int3472-tps68470 i2c-INT3472:06: TPS68470 REVID: 0x21`
- `ov5675 i2c-OVTI5675:00: probe with driver ov5675 failed with error -110`

2. Check binding state:

   `readlink -e /sys/bus/i2c/devices/i2c-INT3472:06/driver || echo int3472-unbound`
   `readlink -e /sys/bus/i2c/devices/i2c-OVTI5675:00/driver || echo ov5675-unbound`

   Observed result:

- `i2c-INT3472:06` is bound to `int3472-tps68470`
- `i2c-OVTI5675:00` is still unbound

3. Check whether any sensor subdevice appeared:

   `find /dev -maxdepth 1 -name 'v4l-subdev*' | sort`

   Observed result:

- no `v4l-subdev*` nodes

4. Additional follow-up review of the same clean-boot journal:

   `journalctl -b -k --no-pager | rg 'Failed to enable|failed to power on|ov5675 i2c-OVTI5675:00|tps68470|TPS68470 REVID'`

   High-value lines:

- `Failed to enable dvdd: -ETIMEDOUT`
- `ov5675 i2c-OVTI5675:00: failed to power on: -110`
- `ov5675 i2c-OVTI5675:00: probe with driver ov5675 failed with error -110`

Interpretation:

- the `ipu-bridge` patch is confirmed good on a clean boot
- the earlier dummy-regulator warnings were not the clean steady-state result
- real regulators are present on a clean boot
- the current failure is now more specific:
  - `ov5675_power_on()` fails
  - the failing regulator enable is `dvdd`
  - the sensor does not even reach the later `failed to find sensor` path

Best current hypothesis:

- the remaining blocker is now in the sensor power-on sequence
- local `ov5675.c` uses `regulator_bulk_enable()` on supplies ordered as:
  - `avdd`
  - `dovdd`
  - `dvdd`
- recovered Windows sequencing for this laptop family is:
  - `VA`
  - `VD`
  - `VSIO`
- a serial Linux power-on sequence matching `avdd -> dvdd -> dovdd` is now the
  most targeted next experiment
